{
  "name": "claude-cloak",
  "version": "0.1.0",
  "description": "Chrome extension that hides Project chats from the Claude sidebar.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
